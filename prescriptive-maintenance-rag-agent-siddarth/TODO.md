# TODO — LangGraph Agent Workflow (agent.py)

## Steps

- [x] Rewrite `agent.py` with an enhanced `AgentState` (LangGraph state graph setup)
- [x] Implement the agent workflow nodes (`agent`, `retrieve`, `execute_tool`, `generate_answer`)
- [x] Connect workflow edges + conditional routing between retrieval and tools (with iteration guard)
- [x] Add `requirements.txt`
- [x] Install dependencies into a fresh `.venv` (`pip install -r requirements.txt`)
- [x] Test the complete execution flow (`python agent.py`) and verify all scenarios PASS

